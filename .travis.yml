language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - pip install --upgrade pip
  - pip install -r requirements.txt
script:
  - python bots-scheduler --help
  - python bots-scheduler --version
